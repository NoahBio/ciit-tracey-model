[pytest]
# Pytest configuration

# Markers for test categorization
markers =
    slow: marks tests as slow (taking more than 1 minute to run)

# Test discovery
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Output options
addopts =
    -v
    --tb=short
    --strict-markers
